// eslint-disable-next-line @typescript-eslint/no-unused-vars
interface LayoutType {
    menu: boolean;
    search: boolean
    back: boolean
    logo: string;
    cart: boolean
    pincode: boolean
}
