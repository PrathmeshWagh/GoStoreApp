
import { SafeAreaView } from 'react-native-safe-area-context';

const SplashScreen = () => {
    return (
        <SafeAreaView>
                   
        </SafeAreaView>
    );
};

export default SplashScreen;
